{
  "name": "fructus-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Off-chain trader SDK mirroring the Fructus on-chain program: instruction builders, typed account decoders, and pure funding/PnL mirrors locked by cross-language vector tests.",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@solana/kit": "8.3.0",
    "@solana/web3.js": "^1.99.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2"
  }
}

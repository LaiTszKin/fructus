{
  "name": "fructus-publisher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Off-chain keeper that feeds the Fructus yield oracle: fetch jitoSOL APY, sign, submit.",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "publish": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@solana/web3.js": "^1.99.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2"
  }
}
